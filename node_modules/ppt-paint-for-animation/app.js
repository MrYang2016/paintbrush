const express = require('express');
const app = express();
const path = require('path');

const port = 8900;

app.use('/', express.static(path.join(__dirname, 'public')));

app.listen(port, err => {
  if (err) return console.log(err);
  console.log(`connect success in http://localhost:${port}`);
});